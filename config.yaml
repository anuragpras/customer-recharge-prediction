data:
  train_path: data/sample_train.csv
  prediction_path: data/sample_prediction.csv

columns:
  target: HasRecharged
  user_id: UserLoginId

model:
  test_size: 0.20
  random_state: 42
  n_estimators: 500
  learning_rate: 0.05
  max_depth: 7
  num_leaves: 31

output:
  output_dir: outputs/latest_run
  model_path: models/lightgbm_0_to_1_model.pkl
  top_percent: 10
